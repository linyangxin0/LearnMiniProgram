<!--pages/cart/childCpns/cart-goodslist/cart-goodslist.wxml-->
<view>
  <block wx:for="{{goodsList}}" wx:key="index">
    <cart-goodsitem goodsItem="{{item}}" bind:checkBottomClick="checkBottomClick" isSelected="{{item.isSelected}}"/>
  </block>
</view>